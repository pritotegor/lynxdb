# file: backend_server.log
# format: json
| let severity = case(level="ERROR", 3, level="WARN", 2, 1=1, 1) | group by service compute avg(severity) as avg_sev, count() as n | order by -avg_sev | take 3