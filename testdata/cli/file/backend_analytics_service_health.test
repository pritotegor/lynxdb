# file: backend_server.log
# format: json
| stats count as requests, avg(duration_ms) as avg_dur, max(duration_ms) as max_dur, count(eval(status>=500)) as errors by service | eval health=if(errors>0, "unhealthy", "healthy") | sort -errors, service | head 5