# file: backend_server.log
# format: json
| bin timestamp span=30m as bucket | stats count as total, count(eval(level="ERROR")) as errors by bucket | eval error_pct=round(errors*100/total, 1) | sort bucket