# .goreleaser.yaml
# GoReleaser configuration for LynxDB
# Docs: https://goreleaser.com
#
# This config builds LynxDB for all supported platforms, generates checksums,
# publishes to GitHub Releases, and uploads to CDN via post-hooks.

version: 2

project_name: lynxdb

before:
  hooks:
    - go mod tidy

builds:
  # ── GNU/glibc builds ────────────────────────────────────────────────────
  - id: lynxdb-gnu
    binary: lynxdb
    main: ./cmd/lynxdb/
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
      - arm
    goarm:
      - "7"
    ignore:
      - goos: windows
        goarch: arm
      - goos: windows
        goarch: arm64
      - goos: darwin
        goarch: arm  # No 32-bit ARM macOS
    ldflags:
      - -s -w
      - -X github.com/lynxbase/lynxdb/internal/buildinfo.Version={{.Version}}
      - -X github.com/lynxbase/lynxdb/internal/buildinfo.Commit={{.ShortCommit}}
      - -X github.com/lynxbase/lynxdb/internal/buildinfo.Date={{.Date}}
    flags:
      - -trimpath

  # ── Musl/static builds (Alpine, distroless) ────────────────────────────
  - id: lynxdb-musl
    binary: lynxdb
    main: ./cmd/lynxdb/
    env:
      - CGO_ENABLED=0
    goos:
      - linux
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w
      - -X github.com/lynxbase/lynxdb/internal/buildinfo.Version={{.Version}}
      - -X github.com/lynxbase/lynxdb/internal/buildinfo.Commit={{.ShortCommit}}
      - -X github.com/lynxbase/lynxdb/internal/buildinfo.Date={{.Date}}
    flags:
      - -trimpath
    # Tag these so archive names include "-musl"
    mod_timestamp: "{{ .CommitTimestamp }}"

archives:
  # ── Standard archives (tar.gz for Unix, zip for Windows) ───────────────
  - id: default
    ids:
      - lynxdb-gnu
    formats: [tar.gz]
    name_template: >-
      lynxdb-v{{ .Version }}-{{ .Os }}-{{ .Arch }}{{ if .Arm }}v{{ .Arm }}{{ end }}
    format_overrides:
      - goos: windows
        formats: [zip]
    files:
      - LICENSE
      - README.md
      - CHANGELOG.md*

  # ── Musl archives ──────────────────────────────────────────────────────
  - id: musl
    ids:
      - lynxdb-musl
    formats: [tar.gz]
    name_template: >-
      lynxdb-v{{ .Version }}-{{ .Os }}-{{ .Arch }}-musl
    files:
      - LICENSE
      - README.md
      - CHANGELOG.md*

checksum:
  name_template: "checksums.txt"
  algorithm: sha256

release:
  draft: false
  prerelease: auto
  name_template: "LynxDB v{{ .Version }}"
  header: |
    ## LynxDB v{{ .Version }}

    ### Quick Install
    ```bash
    curl -fsSL https://lynxdb.org/install.sh | sh
    ```

    ### Checksums
    All artifacts are signed and checksummed. Verify with:
    ```bash
    sha256sum -c checksums.txt
    ```
  footer: |
    ---
    **Full changelog**: https://github.com/lynxbase/lynxdb/compare/{{ .PreviousTag }}...{{ .Tag }}

# ── Homebrew Tap ──────────────────────────────────────────────────────────
brews:
  - name: lynxdb
    ids:
      - default
    repository:
      owner: lynxbase
      name: homebrew-tap
      token: "{{ .Env.HOMEBREW_TAP_GITHUB_TOKEN }}"
    commit_author:
      name: lynxdb-bot
      email: bot@lynxdb.org
    homepage: "https://lynxdb.org"
    description: "Open-source log analytics database. Single binary. SPL2 query language."
    license: "Apache-2.0"
    install: |
      bin.install "lynxdb"
    test: |
      system "#{bin}/lynxdb", "version"

# ── Docker images (GitHub Container Registry) ────────────────────────────
dockers:
  - image_templates:
      - "ghcr.io/lynxbase/lynxdb:{{ .Version }}-amd64"
      - "ghcr.io/lynxbase/lynxdb:latest-amd64"
    use: buildx
    dockerfile: Dockerfile.release
    build_flag_templates:
      - "--platform=linux/amd64"
    ids:
      - lynxdb-gnu
    goarch: amd64

  - image_templates:
      - "ghcr.io/lynxbase/lynxdb:{{ .Version }}-arm64"
      - "ghcr.io/lynxbase/lynxdb:latest-arm64"
    use: buildx
    dockerfile: Dockerfile.release
    build_flag_templates:
      - "--platform=linux/arm64"
    ids:
      - lynxdb-gnu
    goarch: arm64

docker_manifests:
  - name_template: "ghcr.io/lynxbase/lynxdb:{{ .Version }}"
    image_templates:
      - "ghcr.io/lynxbase/lynxdb:{{ .Version }}-amd64"
      - "ghcr.io/lynxbase/lynxdb:{{ .Version }}-arm64"
  - name_template: "ghcr.io/lynxbase/lynxdb:latest"
    image_templates:
      - "ghcr.io/lynxbase/lynxdb:latest-amd64"
      - "ghcr.io/lynxbase/lynxdb:latest-arm64"

# ── Announce ──────────────────────────────────────────────────────────────
changelog:
  sort: asc
  use: github
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^ci:"
      - "^chore:"
  groups:
    - title: "Features"
      regexp: "^feat"
    - title: "Bug Fixes"
      regexp: "^fix"
    - title: "Performance"
      regexp: "^perf"
    - title: "Other"
      order: 999
